<script setup lang="ts">
import { useMapPointsStore } from '@/stores/mapPoints'

defineProps<{
  isEdit: boolean
  isSubmitting: boolean
}>()

const emit = defineEmits(['submit'])
const store = useMapPointsStore()
</script>

<template>
  <div class="px-6 py-4 border-t border-gray-100 bg-gray-50/50 shrink-0 flex items-center justify-end gap-3 rounded-b-2xl">
    <button @click="store.closeModal()" :disabled="isSubmitting"
      class="px-5 py-2.5 text-xs font-black uppercase tracking-widest text-gray-400 bg-white border border-gray-200 rounded-xl hover:bg-gray-50 hover:text-gray-600 transition-all disabled:opacity-50 active:scale-95">
      Batal
    </button>
    <button @click="emit('submit')" :disabled="isSubmitting"
      class="px-6 py-2.5 text-xs font-black uppercase tracking-widest text-white bg-primary rounded-xl hover:bg-primary-dark transition-all shadow-lg shadow-primary/20 active:scale-[0.98] disabled:opacity-50 disabled:cursor-not-allowed flex items-center gap-2">
      <svg v-if="isSubmitting" class="animate-spin h-4 w-4 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
        <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
        <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
      </svg>
      {{ isSubmitting ? 'Memproses...' : (isEdit ? 'Simpan Perubahan' : 'Daftarkan Poin') }}
    </button>
  </div>
</template>
